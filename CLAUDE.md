\# Project: The Orbis Journal



\## Architecture

\- /client — React/TypeScript frontend (Vite)

\- /admin — React/TypeScript admin panel (Vite)

\- /backend — Node.js/Express API



\## Rules (follow strictly to save tokens)

\- NEVER read node\_modules

\- NEVER read package-lock.json or yarn.lock

\- Read only files relevant to the current task

\- Use line ranges when reading large files

\- Do NOT re-read files already read this session

\- Do NOT summarize — just make the edits

\- Skip preambles, go straight to the code

\- Batch related changes into one step



\## Tech Stack

\- Tailwind tokens: brand-navy, brand-yellow, brand-red, ink, ink-muted, paper

\- CSS classes: btn-primary, btn-secondary, admin-input, admin-label

\- Services: articlesAdmin, categoriesAdmin, usersAdmin

\- Fonts: Playfair Display (serif), DM Sans (sans)

